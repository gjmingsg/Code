#include<stdio.h>
#include<string.h>
#define N  1000
int wincount(char** a,len){
	
}
int main(){
	int n,count,i;
	char g[N][2];
	while(scanf("%d",&n)!=EOF){
		for(i=0;i<n;i++)
		    scanf("%s",g[i]);					   
   }	
}
